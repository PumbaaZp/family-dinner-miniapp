# 工作日志

记"每次提交到底改了什么、为什么这么改、怎么验证的"。

写给两周后的自己：那时候只会记得"好像动过点菜页"，不会记得**为什么**要动、以及当时踩过哪个坑。所以这份日志按**提交**组织，而不是按时间流水账。

- 提交时间以 `git log` 为准（`git log --pretty=format:"%h %ad %s" --date=format:"%Y-%m-%d %H:%M"`）
- 每一条都写四个部分：**为什么 / 改了什么 / 关键决定 / 怎么验证**
- 「怎么验证」里的项数口径：脚本末尾那行「通过 N 项，失败 0 项」（`node tools/check.js` 静态、`node tools/test/integration.js` 集成）
- 只记**已经提交进仓库**的东西；本地还没提交的实验不写进来，免得以后对不上
- 日志本身从 **2026-09-12** 起建：这一天把前面几次提交一次性补齐（`3ad7c9e` / `a771dc3` / `ecdc1dd` / `d806b6a`），之后的提交按约定逐条追加

---

## 提交速查

| 日期 | commit | 一句话 | 当时的自检 |
|---|---|---|---|
| 09-11 08:34 | `3ad7c9e` | 初版：整套家宴点菜小程序 + 部署手册 | 静态 87 / 集成 232 |
| 09-12 22:30 | `a771dc3` | 加 `.gitignore`，把本地工具配置从仓库里摘掉 | 同上 |
| 09-12 22:49 | `ecdc1dd` | 每道菜配食材、后台勾采购、三种导出；点菜页加分类分组与滚动联动 | 静态 87 / 集成 236 |
| 09-12 23:18 | `d806b6a` | 「家里的库存」：跟菜谱解耦的食材/调料台账 | 静态 103 / 集成 309 |
| 09-12 23:38 | `dfc8bc7` | 新增工作日志，补齐前面四次提交的记录（纯文档） | 同上 |
| 09-13 21:16 | `a362e8f` | 客人给今晚的菜点赞（一人最多 3 道，下次家宴的参考） | 静态 106 / 集成 349 |
| 09-13 21:5x | `b3594a0` | 点赞改成"一场家宴一盘账"，朋友可回到参加过的某场补投 | 静态 108 / 集成 389 |
| 09-13 21:30 | `d52d680` | 菜品可编辑（菜名/分类/简介/图标/食材/标签）+ 当前场次可改名 | 静态 109 / 集成 403 |
| 09-13 21:36 | `ac016ab` | 把主人加的两道菜补进内置菜库（黄鱼烧年糕 / 油麦菜，67→69 道） | 同上 |
| 09-13 21:49 | `40e5277` | 当前场次的名字以 `config.sessionName` 为准（控制台直接改库也全局生效） | 静态 109 / 集成 407 |
| 09-13 21:57 | `fe10b53` | 云函数调用加 20 秒兜底超时，真机弱网不再无限转圈 | 静态 109 / 集成 409 |
| 09-13 22:25 | `7606105` | 点赞改成**点一下直接生效**（去掉提交按钮），每场最多 3 道 | 静态 109 / 集成 409 |
| 09-13 22:29 | `9f87780` | 主人能看到**每道菜是谁赞的**（朋友端永远拿不到姓名） | **静态 109 / 集成 416** |

> 表里的短哈希是**提交之后**补的（原因见文末约定第 5 条）。要是哪个哈希在 `git log` 里查不到，说明那次提交被 `--amend` 过 —— 以 `git log` 为准，按日期 + 标题就能对上。

---

## 2026-09-13（周日）

### `9f87780` 主人能看到每道菜是谁赞的

**为什么**
主人说：**「主人能看到一个菜的点赞具体是哪些人」**。

但有个前提问题：**点赞记录里只存了 `_openid`，根本没存昵称**。而朋友端的点赞接口还刻意"只返回菜名和人数，不带姓名"（当初是为了隐私）。所以要实现这个需求，得同时解决三件事：
1. 票上得有名字；
2. 名字只能给主人看，不能顺手发到朋友端；
3. 主人已经收到的那些"老票"（没名字）不能显示成一堆"匿名朋友"。

**改了什么**

| 层 | 改动 |
|---|---|
| 数据 | `votes` 文档增加 `nick`：投票时带上当时的昵称，跟订单一样是**快照** |
| 云函数 | `buildLikeTotals` 增加 `withWho` 参数，产出 `who: [{ name, count }]`；只有 `summary`（主人专属）传 `true`，`board` / `votes`（朋友端）一律不传 |
| 兜底 | `buildNickIndex` 用**所有订单**建 `{ 'sessionId|openid': 昵称 }`；老票没昵称时回退到"他那一场的点单昵称"，连订单都没有才叫匿名 |
| 合并 | 同一个人在两场都赞了同一道菜 → 累计榜里合并成一条 `老王×2`（不然名单里会出现两个老王，看着像 bug） |
| 主人端 | 点赞榜每行下面加一行「是谁赞的」，最多显示 6 个，超出写"等 N 人次" |
| 朋友端 | 投票时把昵称一起带上（本地记过的）；**没留名字也能投**，卡片上给一句「主人看不到是谁赞的 —— 点这里留个名字」，点了才弹输入框 |

**关键决定**

1. **不在投票时弹窗问名字**。点赞是"点一下就完事"的动作，第一次点就弹个框会打断手感；所以改成不拦截 + 事后可补（补完自动把已投的票重提交一次，名字就上去了）。
2. **姓名只走 `summary`**：`board` 和 `votes` 是任何朋友都能调的接口，绝不能带 `who`。测试里直接断言"整个响应体里连 `"who"` 这个字符串都不出现"。
3. **票上存昵称快照，而不是每次去 join 订单**：这样跟订单的口径一致（历史记录不因后来改名而变样），而且只投票没点菜的人也有名字。join 只作为**老数据的兜底**。
4. **同名合并成 ×N 而不是列两遍**：累计榜里"老王×2"才读得懂。

**怎么验证**
静态 109 项、集成 **416 项**全部通过。新增覆盖：主人端点赞榜带出名字、朋友端（board/votes）**拿不到 `who`**、同一个人两场都赞 → 合并 ×2、老点赞记录用订单昵称兜底不显示匿名。

**踩到的坑（测试自身的）**：第一版测试用了一个很早抓的 `pick3` 快照，其中一道菜中途被别的用例删了，而 `submitVotes` 对不存在的菜是**静默丢弃**的——于是"投票"变成了空数组、等于"撤销"，失败信息完全误导人。改成用当场查出来的菜 id 才看清真相。这条也说明：**断言里别用陈旧的 fixture**。

---

### `7606105` 点赞改成点一下直接生效（去掉提交按钮）

**为什么**
主人用完点赞后说：**「点赞动作不用手动提交触发，直接点击就生效，最多选当次菜单中的三个菜」**。

原来的设计是"勾选 3 道 → 点提交"——两步。但点赞这个动作本身就是**随手一下**：吃完饭后朋友坐在沙发上顺手点两下，多一个"提交"按钮既多余、又容易让人以为"点了就投好了"（实际没投）。改成点中即存。

**改了什么**

| 层 | 改动 |
|---|---|
| 朋友端 | 点菜＝立即生效；再点＝取消并立即保存。删掉「提交点赞」按钮，保留「清空我在这一场的赞」 |
| 反馈 | 卡片右上角一行轻提示：`保存中… → 已保存 ✓`（失败变红并说明原因），**不弹窗打断** |
| 「几人赞」 | 自己刚投/刚撤的那一票**先在本地算上**，随后静默对齐一次（不然会出现"我明明赞了，还显示 1 人赞"） |
| 范围 | 候选仍然只来自**当次菜单**：当前这一场 = 这一场点过的菜 ∪ 现在上架的菜；历史场次 = 那一场点过的菜 |

**关键决定**

1. **先本地生效、再写云端**（乐观更新）。弱网下点一下要等一秒才变色，用起来就像坏了；所以先改界面再发请求，**失败才回滚**并把错误留在卡片上。
2. **连点保护用"合并"而不是"拦截"**：一个请求还在飞的时候再点，只把最新结果标成 dirty，等它回来再补发**一次**。既不会发出三个乱序请求，也不会因为"正在保存"而让用户的手指被挡住。
3. **上限仍然是服务端说了算**：客户端选满 3 道就提示并拒收第 4 下；服务端也独立校验（`MAX_VOTES = 3`），两边都不许超。
4. **不做"undo 弹窗"**：点错了直接再点一下就取消，比弹一个"已撤销"更符合这个场景。

**怎么验证**
静态 109 项、集成 **409 项**全部通过。新增覆盖：点一下就发请求（不需要提交）、再点一下取消并保存、选满 3 道后第 4 道点不动且不发请求、选满之后仍能取消已选的那道、自己那一票立刻算进"几人赞"、**保存失败会回滚本地勾选并给出错误提示**。

---

### `fe10b53` 云函数调用加 20 秒兜底超时

**为什么**
主人在手机上遇到"一直显示正在加载、开发者工具里正常"。查下来有两类可能，**其中一类正是代码的锅**：真机弱网下 `wx.cloud.callFunction` 有时**既不回调 success 也不回调 fail**，请求就悬着——页面永远等人、刷新重进都没用。开发者工具走电脑网络，几乎不会触发，所以只有手机出问题。

（另一类后来确认是**微信客户端自己卡死**，跟小程序无关；但这一类没法预防，只能兜底。）

**改了什么**
`api.call` 加 20 秒超时 + 只认第一个回调；点菜页把错误**留在页面上**（红色提示条 +「重试」按钮），不再只有一闪而过、根本看不清的 toast。

**关键决定**
- **宁可给一句明确的错误，也不要无限转圈**：用户看到"请求超时，下拉刷新或重试"至少知道该干什么。
- 超时只是不再等待，**不取消底层请求**（也取消不了）——所以用 `settled` 标记保证只认第一个结果，重复回调不会再捣乱。

### `40e5277` 当前场次名字以 `config.sessionName` 为准

**为什么**：主人问"我怎么改这次的名称"。界面入口已经有了，但我发现一个坑——如果**直接在云开发控制台改库**只改 `sessionName`、不动 `sessions[]`，会出现"看板改了、朋友端场次标签还是旧的"（标签读的是 sessions 存档）。于是把当前这一场的名字统一成**只认 `config.sessionName`**，`sessions[]` 退化为纯历史存档。测试里专门模拟了"只改 sessionName"的改库方式。

### `ac016ab` 把主人加的两道菜补进内置菜库

**为什么**：主人报出今天加的两道自定义菜——**黄鱼烧年糕**、**油麦菜**，问"怎么改内容与分类，又不影响朋友点过的和点赞"。
数据在他云上，我够不到，于是：把它们（带分类/简介/图标/食材/标签）**补进 `dishes.seed.js`**（67 → 69 道，蔬菜 +1 / 鱼类 +1），这样以后「重置为默认」「导入内置菜库」都带着它们，还能用「补全食材」一键把食材写进云上那两条同名菜。

**为什么不让他删掉重加**（他自己的疑问）：点赞按 `dishId` 记账，删掉重加会拿到新的 `_id`，**原来的票就断在那儿了**；更糟的是那位朋友下次改票时，服务端会因为"菜不存在"**静默丢掉**那一条。所以「✎ 改」是唯一安全的路——这也是下面 `d52d680` 补编辑入口的原因。

---

### `d52d680` 菜品可编辑（菜名/分类/简介/图标/食材/标签）+ 当前场次可改名

**为什么**
主人说：「更新我今天加的两个自定义菜品的内容与分类信息，更新本次家宴名称为 0913场家宴」。

这两件事**都是云数据库里的数据**，我够不到。但一查代码发现：**这两件事在界面上根本做不到**——

1. 「+ 新增」建的菜只有名字，分类固定写死成 `'自定义'`；而菜单页每行只有**上下架 / 限量 / 删除**，**没有"编辑"入口**。云函数的 `updateDish` 其实早就支持改 name/category/desc/emoji/tags/ingredients，**只是没人调用它**。所以主人加的那两道菜，分类一旦落成「自定义」，就只能删了重加。
2. 场次名字只有「开始新的一场家宴」时能起一次（而且那是**新建**一场），**当前这一场的名字改不了**。

所以这次不是改数据，而是**把缺的两个工具补上**，数据由主人在界面上两下点完。

**改了什么**

| 层 | 改动 |
|---|---|
| 云函数 | 新增 `renameSession`（仅主人）：只改当前场次的 `sessionName`，同步改 `sessions[]` 里这一条；历史场次名字不动 |
| 菜单页 | 每行加 **✎**；点开顶部出编辑面板：菜名 / 分类（下拉＝菜库已有分类）/ 简介 / 图标 / 食材 / 标签；「、」或逗号分隔，食材最多 10 项、标签最多 5 个 |
| 看板 | 顶部加「✎ 改这一场的名字 ›」 |
| 文档 | README 补「编辑菜品」「改这一场的名字」；`真机自测清单.md` 必过项 10 加两行 |

**关键决定**

1. **编辑不动上架状态和限量**：改内容不该顺手把菜下架，也不该把限量抹掉——限量还是在列表里那个「份」框改。这条专门写了断言。
2. **分类下拉用"菜库里已有的分类"**，而不是写死一份列表：主人自己加过什么分类，下拉里就有；另有「自定义」「其他」兜底。
3. **改场次名不碰历史**：只改 `sessionName` 和 `sessions[]` 里对应那一条。要是名字改了历史也跟着变，那"0913场家宴"就会污染上一场（有测试盯着）。
4. 名字沿用 20 字上限（跟昵称、菜名一致）。

**怎么验证**
静态 109 项、集成 **403 项**全部通过。新增覆盖：非管理员不能改场次名 / 不能改菜品、空名字与空 patch 被拒、改完分类真的离开「自定义」且内容全部落库、改内容不动上架与限量、改当前场次名不会串改历史场次名、超长名字被裁剪。

**补记（同一次提交里一起做的）**
主人随后报出了那两道菜的名字：**黄鱼烧年糕**、**油麦菜**。数据在他云上，我把它们**补进了内置菜库**（`dishes.seed.js`，67 → **69 道**，蔬菜 +1 / 鱼类 +1）并重新生成了 `import/my-menu.json`。这样做的好处：

- 以后「重置为默认」「导入内置菜库」都带着它们（同名不会重复导入）
- 可以点「补全食材」把这两道菜的食材**一键写进云上那两条同名菜**（`ingredientsOnly` 只补 `ingredients`，不动分类/上架/限量）
- 分类仍需用新加的 ✎ 手动改（补全食材不管分类）

同时把 README / 真机自测清单 / 菜单页里写死的"67 道"统一改成"69 道"。

**为什么不让主人删掉重加**（他自己问的："怎么改才不会影响朋友点过的和点赞"）：点赞按 `dishId` 记账，删掉重加会拿到新的 `_id`，**原来的票就断在那儿了**；而且下次那位朋友改票时，服务端会因为"菜不存在"静默丢掉那一条。已下的单则只剩快照里的名字（会显示成"已删除的菜"）。所以「✎ 改」是唯一安全的路。

---

### `b3594a0` 点赞改成"一场家宴一盘账"，朋友可回到参加过的某场补投

**为什么**
上午刚上线的点赞是"一个人一份票、全局累计"。主人用完提出三点修正：

1. **点赞榜要以一次家宴为维度** —— 本场谁投了什么，得单独看得见；
2. **朋友能看到自己参加过的某一次，然后进去点赞** —— 吃完散场了才想起来投、或者上次忘了投，要能回去补；
3. **来过多次点赞同一道菜的行为是允许的** —— 这直接推翻了我上午"一个 openid 全局只留一份票"的设计。

第 3 条是关键：上午我把"同一人重复提交=覆盖"当成优点（防止一个人刷榜），但主人要的是**跨场次累加**——"他来过三次、三次都说这道菜好"，这才是 3 票，比单场第一更有说服力。于是必须引入**场次**，把"谁"和"哪一场"一起当成票的身份。

**改了什么**

| 层 | 改动 |
|---|---|
| 数据 | `config` 增 `sessionId` / `sessionNo` / `sessionName` / `sessionStartedAt` / `sessions[]`；`orders` 与 `votes` 都带上 `sessionId`（+ 场次名快照） |
| 云函数 | 新增 `myDinners`（我参加过的场次）、`newSession`（开始新的一场，仅主人）；`votes` / `submitVotes` / `resetVotes` 都接受 `sessionId`；`summary` / `board` 只看当前场次，并额外返回跨场次累计 |
| 朋友端 | 点赞卡片顶部加**场次标签**（`第 1 场` / `中秋家宴`，投过的标「已投 ✓」），点一下切到那一次补投/改票；菜品行的 `👍N` 改成**跨场次累计** |
| 主人端 | 「开始新的一场家宴」入口（看板顶部）；点赞榜拆成**本场榜** + **🏆 累计榜**；「清空本场点赞」只清本场 |
| 菜单页 | 👍N 与"最受欢迎"都用累计（挑下次菜单的参考） |
| 文档 | README 新增「场次：一次家宴一盘账」小节 + 数据模型/常见问题；`真机自测清单.md` 10 项 → **11 项**（新增"开新的一场 + 场次回顾"） |

**关键决定**

1. **一人一票的含义变成"一场一人一票"**：同一场里重复提交仍然只覆盖自己（本场榜不会被刷），但**不同场次各投各的**（累计榜累加）。两句话都对，取决于看哪张榜。
2. **不开新场就永远是第 1 场**：`sessionId` 默认 `'party'`，而**老数据里根本没有 `sessionId` 字段**。代码把"缺这个字段"等价成第 1 场，于是**不需要任何数据迁移**——朋友刚投的那些票、主人已经下过的单，升级上来自动归位到第 1 场。（有测试专门塞了一条没有 `sessionId` 的订单验证这点，包括"它不会串到第 2 场去"。）
3. **场次是归档，不是删除**：开新场只切换 `sessionId` + 重置「已采购」勾选；上一场的订单和点赞**原样留着**（朋友要能回去投、累计榜要能算）。`pantry`（家里有什么）当然也不动。
4. **看板只看当前这一场**：不然"今晚几个人、几道菜"永远混着上一场的数字。历史的看板数据没有做界面，需要时直接查 `orders` 集合。
5. **候选菜按场次算**：当前场次 = 本场订单菜 ∪ 我投过的菜 ∪ **当前上架的菜**；历史场次 = 本场订单菜 ∪ 我投过的菜（**不含**现在上架的菜——那是"现在的菜单"，跟三天前那场没关系）。
6. **只能给自己参加过的场次投票**（点过单或投过票），当前这一场谁都能投（有人只来喝酒没点单，也该能投）。

**怎么验证**
静态 108 项、集成 **389 项**全部通过。新增的测试覆盖：开场只允许主人、序号/名字正确、新场看板归零、旧场订单保留、同人同菜跨场投两次 → 累计 2 票 / 本场 1 票、回旧场改票只影响那一场、没参加过的场次被拒、`resetVotes` 按场次只清一场、老数据归位到第 1 场且不串场。

**遗留**
- 没有"历史场次的看板回看"界面（数据都在，只是没做页面）
- 累计榜没有按"最近 N 场"加权的概念（现在是全历史累加，场次多了以后可能想看近况）

---

### `a362e8f` 客人给今晚的菜点赞（一人最多 3 道，下次家宴的参考）

> 这是同一天更早的一次提交。它的第 3 条关键决定（"一人一票"）在当天晚些时候被 `b3594a0` 修正成了"一场一人一票"，理由见上面那一条。

**为什么**
家宴办完之后主人提的需求：**「希望客人能给点的菜进行打分，将菜单中选择三个菜进行点赞，后续的家宴给新朋友点餐时可以做参考」**。

背景是这次家宴已经顺利结束，主人发现"哪道菜受欢迎"这件事只在脑子里，下次请新朋友还是凭感觉挑菜。所以要做的是**一个跨场次累积的口碑数据**，而不是"本场打分"：

- 客人端：吃完之后回小程序，从今晚吃到的菜里挑最多 3 道点赞
- 主人端：下次挑菜单时，把"上次几人赞"和"家里食材够不够"放在一起看

**改了什么**

| 层 | 改动 |
|---|---|
| 数据 | **新集合 `votes`**，一人一份文档：`{ _openid, items[{dishId,name,emoji}]（≤3）, createdAt, updatedAt }` |
| 云函数 | 新增 `votes`（读：候选名单 + 我投了哪些 + 合计）、`submitVotes`（覆盖式写入，空数组=撤销）、`resetVotes`（主人清空）；`summary` 与 `board` 都给每行菜带上 `likeCount` |
| 朋友端 | 点菜页新增「👍 给今晚的菜点个赞」卡片（可折叠、3 个名额、有选中态）；菜品行与「大家都在点什么」都显示 `👍N` |
| 主人端 | 后厨看板新增「👍 点赞榜」（按人数排序、前三名奖牌、进度条）+ 菜品汇总每行 👍N + 「清空点赞」 |
| 菜单页 | 每道菜名下显示 `👍N`，顶部列出"上次最受欢迎的几道"——**这条直接服务于"下次挑菜做参考"** |
| 文档 | README 功能清单/数据模型/已验证行为、`真机自测清单.md`（9 项 → **10 项**，新增"客人点赞"一节） |

**关键决定**

1. **一人一票，不是一次一票**：同一个 openid 重复提交是**覆盖**。否则同一个人每次家宴都投一次，几次下来"10 人推荐"全是他自己 —— 那样当参考就废了。代价是"历史累计"丢失，但换来的"几人推荐"是真实的。
2. **不受点单截止时间限制**：点赞本来就是吃完才做的事。点单锁了、甚至主人把菜一键全部下架了，**投票必须还能用**——所以候选名单取的是「**订单里出现过的菜** ∪ 已上架的菜」，而不是"上架的菜"。这是这一版最重要的一条设计，专门写了测试盯着（"全部下架后仍然能点赞 / 候选名单还在"）。
3. **不做"场次"概念**：本来想过加 `round`（第几场）让老票留档，但那会多出一个主人要维护的开关。现在这样"同一人下次再投即更新口味偏好"，零维护成本。
4. **点赞只存 dishId + 菜名快照**：菜被删了也还显示原名（跟订单汇总同一口径），不会变成一堆 `undefined`。
5. **`votes` 集合要能自动补建**：`init` 只在"开通家宴"时跑一次，**已经初始化过的环境不会再执行 `ensureCollections`**。不兜住的话，老环境里第一次点赞直接报 `collection not exists` —— 所以加了 `withCollection()`：遇到 -502005 就补建一次再重试（有测试专门验这条）。

**怎么验证**
静态 106 项、集成 **349 项**全部通过。测试逼出**一个真实 bug**：

- **超限判断写在了去重之前**：客户端传 `[A, B, C, A]`（带重复的合法请求）会被误判成"投了 4 道"而拒绝。改成**先去掉重复和无效应答、再判上限**。（这条是写测试时顺手构造了"带重复 + 不存在"的数组才暴露出来的。）

另外顺手修了测试自身的问题：投票测试最初放在下单之前，而"候选名单来自订单"这条断言需要订单存在 —— 改成在测试块内用**临时 openid 下单、块末尾撤销**，既验证了规则又不污染后面的下单测试。

**遗留**
- ~~没有"每次家宴单独统计"~~ —— 当天晚些时候由 `b3594a0` 补上（引入场次后，本场榜和累计榜分开了）
- 没有导出「点赞榜」文本（看板上直接看/截图即可，需要的话再加一个复制按钮）

---

## 2026-09-12（周六）

这一天连着三次提交，做的事是"把菜谱、采购、家里的存货这三件事串起来"。

### `d806b6a` 家里的库存（跟菜谱解耦的食材/调料台账）

**为什么**
主人提的需求原话是：「我可以将家里有的食材、调料等库存录入系统，**不一定要跟菜谱关联**，我后续可以看我有哪些食材再去做菜单添加」。

这句话点出了上一版（`ecdc1dd`）的设计偏差：那时"食材"是挂在**菜谱**上的（`dishes.ingredients`，表示"这道菜要用什么"），是**菜谱的一部分**；而主人要的是**独立的台账**（"我家现在有什么"）。两者只有名字相同，语义完全不同 —— 把"我家有什么"塞进菜谱字段里就错了。

**改了什么**

| 层 | 改动 |
|---|---|
| 数据 | 存在 `config` 文档的 `pantry` 字段：`{ "生抽": { cat, qty, note }, ... }`，**不新开集合** |
| 云函数 | 新增 4 个 action：`listPantry` / `savePantryItems` / `removePantryItem` / `clearPantry`（全部仅主人）；`summary` 返回整份 `pantry`，并给每项食材加 `inStock` |
| 新页面 | `pages/admin/pantry/`（+ `miniprogram/utils/pantry.js` 纯逻辑）；入口在「我的点单 → 主人控制台 → 🧺 家里的库存」和「家宴设置与菜库」的按钮 |
| 菜单页 | 每道菜名下标出 `✅ 食材都有` / `🧺 缺 N 样`，点一下看具体缺哪几样并能跳去补库存；顶部给"有 N 道菜食材齐全" |
| 看板 | 食材清单分 `还要买 / 已买 / 家里有` 三档，家里有的带标记排最后、不计入待买；「复制待购食材」自动扣掉家里有的 |
| 文档 | README 功能清单/数据模型/常见问题、`真机自测清单.md`（8 项 → **9 项**，顺手把过期的"36 道"改成 67 道、复制按钮改成三种） |

**关键决定**（这几条以后想改的话，先回来看理由）

1. **库存塞进 `config` 而不是新开集合**：库存天然是"一份"而不是"很多条记录"；而且不新开集合就少一个要建的东西、少一份权限要操心。
2. **库存只做标记，不把食材从清单里删掉**：采购清单表达的是"这次家宴要用到什么"，家里有没有是另一件事。删掉就看不全了，标出来才能一眼看出少什么。
3. **「家里有 / 已买 / 还要买」三个数刻意设计成可以相加**（`total = inStock + purchased + missing`，`purchased` 不含家里有的）。既勾了"已采购"又是"家里有"时只算家里有，不重复计数 —— 有专门的测试盯着这条。
4. **只做逐字同名匹配，不猜同义词**：`小葱` ≠ `葱`、`生抽` ≠ `薄盐生抽`。宁可多买一次，也别把不一样的当成一样的。为了让名字天然对齐，库存页给了「从菜谱食材快速点选」——把那 93 种食材列成可点的标签，名字直接来自菜谱。
5. **库存是主人私事，朋友看不到**：`publicConfig` 不含 `pantry`，朋友端也没入口。

**怎么验证**
静态 103 项、集成 **309 项**全部通过。新写的测试当场逼出**两个真实 bug**（不是"看起来对"，是被断言挡下来的）：

1. **更新库存时只传数量会把备注冲掉** —— 原来的实现整条替换。改成"**没传的字段保持不变，传空字符串才是清空**"，这样"快速点选"这种只带名字的调用不会抹掉已有数据，而编辑表单（每个字段都会传）依然能正常清空。
2. **库存是空的或没读到的时候，菜单页每道菜都挂一句"缺 N 样"** —— 满屏噪音，还像数据丢了。改成库存为空时一律不显示，并且加了"云函数还是旧版（没有 `listPantry`）时菜单页照常加载、只提示一句可能没重新部署"的测试。

**遗留**
- 库存只做标记，**不扣减**：做一次家宴不会自动把用掉的食材从库存里扣掉（README 第九节已列为后续方向）
- 没有保质期/临期提醒

### `ecdc1dd` 每道菜配置食材清单，后台可勾选采购状态，导出三种清单

**为什么**
主人连着提了三件事：①「菜谱增加食材配置，后台可以选择食材是否已采购」；②「一键复制不需要关注谁点的，只要导出菜谱清单」；③「主食加一道小米南瓜粥」。
顺带把上一轮**已经改好但还没提交**的点菜页分类体验一起提交了（当时工作区里有未提交的改动，所以这个 commit 的内容比标题看起来多）。

**改了什么**

| 层 | 改动 |
|---|---|
| 菜库 | 67 道菜（含新增主食「小米南瓜粥」，排序 650）**全部配上 `ingredients`**：234 条、去重 93 种、平均 3.49 条/道 |
| 云函数 | `seedDishes` 新增 `ingredientsOnly`（只补食材，**不动上架状态/限量/改过的描述**）；`buildIngredients` 按"已上架 ∪ 订单出现过的菜"聚合去重；新增 `toggleIngredient` / `resetShopping` |
| 看板 | 食材采购区（买到了点一下划掉、未买的排前面）；**一键复制拆成三种**：菜谱清单 / 菜+食材 / 待购食材，**都不含"谁点的"** |
| 菜单页 | 新增「补全食材」按钮（给老菜库非破坏性补字段） |
| 点菜页 | 分类标签带每类菜数；选「全部」时每类前插一条分组标题；**往下滑时顶部分类高亮跟着变**（scroll-spy：量一次标题绝对偏移 + 吸顶栏高度，`onPageScroll` 只做数字比较） |
| 工具 | `tools/validate-menu.js` 把 `ingredients` 纳入校验（含调料黑名单，盐/油/水这类不进食材清单） |

**关键决定**

1. **「全部时插入分组标题」是为了解决"往下划后看不到分类"**：只靠 sticky 分类栏在某些机型上不保险，分组标题是不依赖 sticky 的保底方案。
2. **「高亮」与「过滤」彻底分开**：`activeCat` 只管过滤，`viewCat` 只管高亮。早期把两者混在一起，结果"滚动到猪肉类"时会当作点了筛选，列表突然只剩猪肉 —— 这是个硬约束，别再合回去。
3. **三种导出都不含点单人**，且加了回归断言：文本里出现点单人/按人明细/主人自己的单，测试就失败。
4. **复制文案里不出现库存的备注和分类**：那是主人自己的家事。

**怎么验证**
静态 87 项、集成 **236 项**（后来并入 `d806b6a` 的 309 项里）。
过程中修掉的坑：dashboard 里重复声明 `cfg`（`Identifier 'cfg' has already been declared`）；`.ing*` 样式类没定义；**食材清单为空** —— 根因是云函数 `seedDishes` 的白名单把 `ingredients` 字段丢掉了（`seedDishes` / `addDish` / `updateDish` / `listDishes` / `aggregateOrders` 都要放行这个字段，漏一个就白配）。

### `a771dc3` chore: add .gitignore, untrack local devtools config

**为什么**
仓库是公开的，而 `project.private.config.json` 是**每台机器一份**的本地开发者工具配置（会随工具版本和调试设置变化），提交上去只会制造无意义的 diff 和冲突。

**改了什么**
新增 `.gitignore`：忽略 `project.private.config.json`、`node_modules/`、根目录的 `*.jpg|jpeg|png` 等；把已被跟踪的 `project.private.config.json` 从索引里移除（文件仍留在本地）。

**关键决定**
- 仓库里那张 `微信图片_20260910224653_3_120.jpg` **保留**：主人确认过"图片与邮箱不涉及敏感信息，不需要处理"（已核对无 EXIF/GPS）。
- **千万不要在 `D:\ai\DeepSeek` 顶层 `git init`**：那个目录里有带手机号的简历等私人文件，只在这个子目录里做版本管理。

**怎么验证**
`git status` 干净，`project.private.config.json` 消失但本地文件还在，`node tools/check.js` 仍全绿（它专门校验这个文件是合法 JSON，所以文件必须留在本地）。

---

## 2026-09-11（周五）

### `3ad7c9e` version init

**为什么**
项目起点：主人要在本周末（09-12 / 09-13）请朋友上门吃家宴，需要一个"扫码就能点菜"的东西，并且评估过外包/现成工具的方案后选了**自研微信小程序 + 云开发**（免备案、免服务器、免域名）。

**改了什么**
39 个文件、6544 行，一次成型：

- 云函数 `cloudfunctions/api/`：**唯一一个云函数**走 `action` 路由（部署时只右键上传一次，减少出错点）
- 朋友端 `pages/index/`：分类滑选、加减份数、每道菜备注、提交昵称/人数/到达时间/忌口、随时改单、一键撤销、限量校验、截止锁定
- 主人端 `pages/admin/`：菜库管理（整组上下架、逐道限量、临时加菜）+ 后厨看板（汇总、按人明细、一键复制）
- `pages/mine/`：我的点单 + 主人入口（主人口令）
- 工具：`tools/check.js`（静态自检）、`tools/test/integration.js`（离线集成测试，自建 mock 的 `wx-server-sdk`）、`tools/validate-menu.js`（菜库数据校验）
- 文档：`README.md`（部署手册 + 功能清单 + 数据模型 + 常见问题）、`兜底方案.md`（零代码备选：群接龙/腾讯文档）、`真机自测清单.md`

**关键决定**（都是踩过或差点踩的坑）

1. **所有读写都走云函数**：云开发控制台的集合权限不用动，也不会出现"客户端能改别人的单"。
2. **`saveConfig` 不依赖"`update` 在文档不存在时会抛错"**：云开发 SDK 行为并不统一 —— 有的抛错，有的只静默返回 `stats.updated = 0`。早期版本按"一定抛错"写，第一次初始化时创建分支根本走不到，紧接着读回来是 `null`，就崩在 `cfg.admins` 上。改成"先查存在性，再 update，否则 set"，并加了"静默模式"回归测试。
3. **`<input>` 是原生组件，不能用垂直 padding**：会表现为"文字掉到框下面一半，点一下才正常"。统一改成显式 `height` + 只留水平 padding，并且 `node tools/check.js` 会把这种写法**拦在提交之前**。
4. **`wx:for` 必须带 `wx:key`，且不能拿昵称当 key**：家宴里两个人把昵称写成一样完全可能，重复 key 会让列表渲染错乱 —— 一律用下标派生的唯一值。

**怎么验证**
静态 **87 项**、集成 **232 项**（含"SDK 两种行为下初始化都不崩"的兼容性回归）。

---

## 现在到哪了 / 下一步

**代码状态**：`master` 本地领先 `origin/master` 若干提交，等主人自己 `git push`（远端操作在开发机上受限）。

**主人在自己电脑上还要做的**（按顺序）

1. 右键 `cloudfunctions/api` →「上传并部署：云端安装依赖」← **必须做**，否则食材与库存相关功能会报"未知操作"
2. 点工具栏「编译」
3. 「家宴设置与菜库」→ 点「补全食材」（只给旧菜库补 `ingredients`，不动上架状态和限量）
4. 照 `真机自测清单.md` 走一遍 **9 项必过**（约 25 分钟）
5. 上传 → 后台选为体验版 → 生成二维码 → 把朋友加为体验成员后发码

**还可以往下做的方向**

- 菜品图片（云存储 + 上传）
- 库存记消耗：做一次家宴自动把用掉的食材从库存里扣掉（现在只做标记、不扣减）
- 库存加保质期/临期提醒

---

## 维护这份日志的约定

1. **一次提交一条**，标题直接用 commit 的 `--oneline`，不要另起名字（方便回头 `git show` 对照）。
2. 四个部分都要写，**「为什么」不能省** —— 这是这份日志唯一无法从代码里恢复的信息。
3. 「怎么验证」写**具体项数和被测试挡下来的 bug**；只写"已测试"等于没写。
4. 顺手把过期数字改掉（菜库道数、自测清单项数、README 里的项数），别留着互相矛盾。
5. **短哈希要在提交之后补**，而且**补哈希这一下必须放在下一次提交里**。
   原因很实在：哈希只能提交后才知道，写进自己那次提交里就必然要 `amend`，而 `amend` 会**再改一次哈希** —— 死循环。所以：先把这次提交做掉，然后把哈希补进表里，跟着下一次提交一起上去；只补哈希的那种小改动不必单开一条记录（沿用上一条即可）。
6. **纯文档提交也要记一条**（一句话即可），否则以后看 `git log` 会不知道某天为什么冒出一堆 .md 改动。
   唯一的例外是本文件自己诞生那一次 —— 它就是这份日志的起点，无法在自己的内容里写自己的 hash。
7. 只记**已经提交**的东西。工作区里改了一半的实验不写，免得以后对不上。
