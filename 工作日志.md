# 工作日志

记"每次提交到底改了什么、为什么这么改、怎么验证的"。

写给两周后的自己：那时候只会记得"好像动过点菜页"，不会记得**为什么**要动、以及当时踩过哪个坑。所以这份日志按**提交**组织，而不是按时间流水账。

- 提交时间以 `git log` 为准（`git log --pretty=format:"%h %ad %s" --date=format:"%Y-%m-%d %H:%M"`）
- 每一条都写四个部分：**为什么 / 改了什么 / 关键决定 / 怎么验证**
- 「怎么验证」里的项数口径：脚本末尾那行「通过 N 项，失败 0 项」（`node tools/check.js` 静态、`node tools/test/integration.js` 集成）
- 只记**已经提交进仓库**的东西；本地还没提交的实验不写进来，免得以后对不上
- 日志本身从 **2026-09-12** 起建：这一天把前面几次提交一次性补齐（`3ad7c9e` / `a771dc3` / `ecdc1dd` / `d806b6a`），之后的提交按约定逐条追加

---

## 提交速查

| 日期 | commit | 一句话 | 当时的自检 |
|---|---|---|---|
| 09-11 08:34 | `3ad7c9e` | 初版：整套家宴点菜小程序 + 部署手册 | 静态 87 / 集成 232 |
| 09-12 22:30 | `a771dc3` | 加 `.gitignore`，把本地工具配置从仓库里摘掉 | 同上 |
| 09-12 22:49 | `ecdc1dd` | 每道菜配食材、后台勾采购、三种导出；点菜页加分类分组与滚动联动 | 静态 87 / 集成 236 |
| 09-12 23:18 | `d806b6a` | 「家里的库存」：跟菜谱解耦的食材/调料台账 | **静态 103 / 集成 309** |

---

## 2026-09-12（周六）

这一天连着三次提交，做的事是"把菜谱、采购、家里的存货这三件事串起来"。

### `d806b6a` 家里的库存（跟菜谱解耦的食材/调料台账）

**为什么**
主人提的需求原话是：「我可以将家里有的食材、调料等库存录入系统，**不一定要跟菜谱关联**，我后续可以看我有哪些食材再去做菜单添加」。

这句话点出了上一版（`ecdc1dd`）的设计偏差：那时"食材"是挂在**菜谱**上的（`dishes.ingredients`，表示"这道菜要用什么"），是**菜谱的一部分**；而主人要的是**独立的台账**（"我家现在有什么"）。两者只有名字相同，语义完全不同 —— 把"我家有什么"塞进菜谱字段里就错了。

**改了什么**

| 层 | 改动 |
|---|---|
| 数据 | 存在 `config` 文档的 `pantry` 字段：`{ "生抽": { cat, qty, note }, ... }`，**不新开集合** |
| 云函数 | 新增 4 个 action：`listPantry` / `savePantryItems` / `removePantryItem` / `clearPantry`（全部仅主人）；`summary` 返回整份 `pantry`，并给每项食材加 `inStock` |
| 新页面 | `pages/admin/pantry/`（+ `miniprogram/utils/pantry.js` 纯逻辑）；入口在「我的点单 → 主人控制台 → 🧺 家里的库存」和「家宴设置与菜库」的按钮 |
| 菜单页 | 每道菜名下标出 `✅ 食材都有` / `🧺 缺 N 样`，点一下看具体缺哪几样并能跳去补库存；顶部给"有 N 道菜食材齐全" |
| 看板 | 食材清单分 `还要买 / 已买 / 家里有` 三档，家里有的带标记排最后、不计入待买；「复制待购食材」自动扣掉家里有的 |
| 文档 | README 功能清单/数据模型/常见问题、`真机自测清单.md`（8 项 → **9 项**，顺手把过期的"36 道"改成 67 道、复制按钮改成三种） |

**关键决定**（这几条以后想改的话，先回来看理由）

1. **库存塞进 `config` 而不是新开集合**：库存天然是"一份"而不是"很多条记录"；而且不新开集合就少一个要建的东西、少一份权限要操心。
2. **库存只做标记，不把食材从清单里删掉**：采购清单表达的是"这次家宴要用到什么"，家里有没有是另一件事。删掉就看不全了，标出来才能一眼看出少什么。
3. **「家里有 / 已买 / 还要买」三个数刻意设计成可以相加**（`total = inStock + purchased + missing`，`purchased` 不含家里有的）。既勾了"已采购"又是"家里有"时只算家里有，不重复计数 —— 有专门的测试盯着这条。
4. **只做逐字同名匹配，不猜同义词**：`小葱` ≠ `葱`、`生抽` ≠ `薄盐生抽`。宁可多买一次，也别把不一样的当成一样的。为了让名字天然对齐，库存页给了「从菜谱食材快速点选」——把那 93 种食材列成可点的标签，名字直接来自菜谱。
5. **库存是主人私事，朋友看不到**：`publicConfig` 不含 `pantry`，朋友端也没入口。

**怎么验证**
静态 103 项、集成 **309 项**全部通过。新写的测试当场逼出**两个真实 bug**（不是"看起来对"，是被断言挡下来的）：

1. **更新库存时只传数量会把备注冲掉** —— 原来的实现整条替换。改成"**没传的字段保持不变，传空字符串才是清空**"，这样"快速点选"这种只带名字的调用不会抹掉已有数据，而编辑表单（每个字段都会传）依然能正常清空。
2. **库存是空的或没读到的时候，菜单页每道菜都挂一句"缺 N 样"** —— 满屏噪音，还像数据丢了。改成库存为空时一律不显示，并且加了"云函数还是旧版（没有 `listPantry`）时菜单页照常加载、只提示一句可能没重新部署"的测试。

**遗留**
- 库存只做标记，**不扣减**：做一次家宴不会自动把用掉的食材从库存里扣掉（README 第九节已列为后续方向）
- 没有保质期/临期提醒

### `ecdc1dd` 每道菜配置食材清单，后台可勾选采购状态，导出三种清单

**为什么**
主人连着提了三件事：①「菜谱增加食材配置，后台可以选择食材是否已采购」；②「一键复制不需要关注谁点的，只要导出菜谱清单」；③「主食加一道小米南瓜粥」。
顺带把上一轮**已经改好但还没提交**的点菜页分类体验一起提交了（当时工作区里有未提交的改动，所以这个 commit 的内容比标题看起来多）。

**改了什么**

| 层 | 改动 |
|---|---|
| 菜库 | 67 道菜（含新增主食「小米南瓜粥」，排序 650）**全部配上 `ingredients`**：234 条、去重 93 种、平均 3.49 条/道 |
| 云函数 | `seedDishes` 新增 `ingredientsOnly`（只补食材，**不动上架状态/限量/改过的描述**）；`buildIngredients` 按"已上架 ∪ 订单出现过的菜"聚合去重；新增 `toggleIngredient` / `resetShopping` |
| 看板 | 食材采购区（买到了点一下划掉、未买的排前面）；**一键复制拆成三种**：菜谱清单 / 菜+食材 / 待购食材，**都不含"谁点的"** |
| 菜单页 | 新增「补全食材」按钮（给老菜库非破坏性补字段） |
| 点菜页 | 分类标签带每类菜数；选「全部」时每类前插一条分组标题；**往下滑时顶部分类高亮跟着变**（scroll-spy：量一次标题绝对偏移 + 吸顶栏高度，`onPageScroll` 只做数字比较） |
| 工具 | `tools/validate-menu.js` 把 `ingredients` 纳入校验（含调料黑名单，盐/油/水这类不进食材清单） |

**关键决定**

1. **「全部时插入分组标题」是为了解决"往下划后看不到分类"**：只靠 sticky 分类栏在某些机型上不保险，分组标题是不依赖 sticky 的保底方案。
2. **「高亮」与「过滤」彻底分开**：`activeCat` 只管过滤，`viewCat` 只管高亮。早期把两者混在一起，结果"滚动到猪肉类"时会当作点了筛选，列表突然只剩猪肉 —— 这是个硬约束，别再合回去。
3. **三种导出都不含点单人**，且加了回归断言：文本里出现点单人/按人明细/主人自己的单，测试就失败。
4. **复制文案里不出现库存的备注和分类**：那是主人自己的家事。

**怎么验证**
静态 87 项、集成 **236 项**（后来并入 `d806b6a` 的 309 项里）。
过程中修掉的坑：dashboard 里重复声明 `cfg`（`Identifier 'cfg' has already been declared`）；`.ing*` 样式类没定义；**食材清单为空** —— 根因是云函数 `seedDishes` 的白名单把 `ingredients` 字段丢掉了（`seedDishes` / `addDish` / `updateDish` / `listDishes` / `aggregateOrders` 都要放行这个字段，漏一个就白配）。

### `a771dc3` chore: add .gitignore, untrack local devtools config

**为什么**
仓库是公开的，而 `project.private.config.json` 是**每台机器一份**的本地开发者工具配置（会随工具版本和调试设置变化），提交上去只会制造无意义的 diff 和冲突。

**改了什么**
新增 `.gitignore`：忽略 `project.private.config.json`、`node_modules/`、根目录的 `*.jpg|jpeg|png` 等；把已被跟踪的 `project.private.config.json` 从索引里移除（文件仍留在本地）。

**关键决定**
- 仓库里那张 `微信图片_20260910224653_3_120.jpg` **保留**：主人确认过"图片与邮箱不涉及敏感信息，不需要处理"（已核对无 EXIF/GPS）。
- **千万不要在 `D:\ai\DeepSeek` 顶层 `git init`**：那个目录里有带手机号的简历等私人文件，只在这个子目录里做版本管理。

**怎么验证**
`git status` 干净，`project.private.config.json` 消失但本地文件还在，`node tools/check.js` 仍全绿（它专门校验这个文件是合法 JSON，所以文件必须留在本地）。

---

## 2026-09-11（周五）

### `3ad7c9e` version init

**为什么**
项目起点：主人要在本周末（09-12 / 09-13）请朋友上门吃家宴，需要一个"扫码就能点菜"的东西，并且评估过外包/现成工具的方案后选了**自研微信小程序 + 云开发**（免备案、免服务器、免域名）。

**改了什么**
39 个文件、6544 行，一次成型：

- 云函数 `cloudfunctions/api/`：**唯一一个云函数**走 `action` 路由（部署时只右键上传一次，减少出错点）
- 朋友端 `pages/index/`：分类滑选、加减份数、每道菜备注、提交昵称/人数/到达时间/忌口、随时改单、一键撤销、限量校验、截止锁定
- 主人端 `pages/admin/`：菜库管理（整组上下架、逐道限量、临时加菜）+ 后厨看板（汇总、按人明细、一键复制）
- `pages/mine/`：我的点单 + 主人入口（主人口令）
- 工具：`tools/check.js`（静态自检）、`tools/test/integration.js`（离线集成测试，自建 mock 的 `wx-server-sdk`）、`tools/validate-menu.js`（菜库数据校验）
- 文档：`README.md`（部署手册 + 功能清单 + 数据模型 + 常见问题）、`兜底方案.md`（零代码备选：群接龙/腾讯文档）、`真机自测清单.md`

**关键决定**（都是踩过或差点踩的坑）

1. **所有读写都走云函数**：云开发控制台的集合权限不用动，也不会出现"客户端能改别人的单"。
2. **`saveConfig` 不依赖"`update` 在文档不存在时会抛错"**：云开发 SDK 行为并不统一 —— 有的抛错，有的只静默返回 `stats.updated = 0`。早期版本按"一定抛错"写，第一次初始化时创建分支根本走不到，紧接着读回来是 `null`，就崩在 `cfg.admins` 上。改成"先查存在性，再 update，否则 set"，并加了"静默模式"回归测试。
3. **`<input>` 是原生组件，不能用垂直 padding**：会表现为"文字掉到框下面一半，点一下才正常"。统一改成显式 `height` + 只留水平 padding，并且 `node tools/check.js` 会把这种写法**拦在提交之前**。
4. **`wx:for` 必须带 `wx:key`，且不能拿昵称当 key**：家宴里两个人把昵称写成一样完全可能，重复 key 会让列表渲染错乱 —— 一律用下标派生的唯一值。

**怎么验证**
静态 **87 项**、集成 **232 项**（含"SDK 两种行为下初始化都不崩"的兼容性回归）。

---

## 现在到哪了 / 下一步

**代码状态**：`master` 本地领先 `origin/master` 若干提交，等主人自己 `git push`（远端操作在开发机上受限）。

**主人在自己电脑上还要做的**（按顺序）

1. 右键 `cloudfunctions/api` →「上传并部署：云端安装依赖」← **必须做**，否则食材与库存相关功能会报"未知操作"
2. 点工具栏「编译」
3. 「家宴设置与菜库」→ 点「补全食材」（只给旧菜库补 `ingredients`，不动上架状态和限量）
4. 照 `真机自测清单.md` 走一遍 **9 项必过**（约 25 分钟）
5. 上传 → 后台选为体验版 → 生成二维码 → 把朋友加为体验成员后发码

**还可以往下做的方向**

- 菜品图片（云存储 + 上传）
- 库存记消耗：做一次家宴自动把用掉的食材从库存里扣掉（现在只做标记、不扣减）
- 库存加保质期/临期提醒

---

## 维护这份日志的约定

1. **一次提交一条**，标题直接用 commit 的 `--oneline`，不要另起名字（方便回头 `git show` 对照）。
2. 四个部分都要写，**「为什么」不能省** —— 这是这份日志唯一无法从代码里恢复的信息。
3. 「怎么验证」写**具体项数和被测试挡下来的 bug**；只写"已测试"等于没写。
4. 顺手把过期数字改掉（菜库道数、自测清单项数、README 里的项数），别留着互相矛盾。
5. **纯文档提交也要记一条**（一句话即可），否则以后看 `git log` 会不知道某天为什么冒出一堆 .md 改动。
   唯一的例外是本文件自己诞生那一次 —— 它就是这份日志的起点，无法在自己的内容里写自己的 hash。
6. 只记**已经提交**的东西。工作区里改了一半的实验不写，免得以后对不上。
