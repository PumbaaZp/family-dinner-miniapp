# 真机验收清单（开饭前自测，约 35 分钟）

部署完成后**不要直接发给朋友**。先用这份清单在自己手机上跑一遍，11 个必过项全绿再发码。

- 自测方式：开发者工具点「预览」→ 手机微信扫码（**用你自己的微信号即可，不需要加体验成员**）
- 涉及"朋友视角"的步骤，最好用**第二台手机或第二个微信号**；只有一个号也能测，但要点单后记得撤销（见第 6 项）
- 每项都有「期望结果」和「不通过先看哪里」，不要跳过
- ⚠️ **本轮更新过云函数**（新增场次与点赞相关 action）：自测前先在开发者工具里右键 `cloudfunctions/api` →「上传并部署：云端安装依赖」，再点一次「编译」
- 💡 第 9、10 两项都跟"场次"有关，**建议连着做完**：先投一轮票，再开新场，然后回顾旧场次

---

## 必过项 1：首次开通

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 手机扫码进入，切到「我的点单」 | 显示「家宴还没开通」+ 一个开通按钮 | 页面白屏 → 云函数没部署；提示"云函数 api 还没部署" → 回到 README 第 5 步 |
| 点「我是主人，一键开通」 | 弹确认框 → 确认后弹出一个「开通成功」窗口，**里面直接写着 4 位主人口令**，点「去设置菜库」可直接跳转 | 提示「主人口令不正确」→ 说明用的还是旧代码，重新上传部署云函数 |
| 回到「点菜」页 | 顶部有「家宴设置与菜库 ›」和「后厨看板 ›」两个入口；菜单为空时还会有一个「去上架今晚的菜」按钮 | 显示"家宴还没开通" → 下拉刷新一次 |

## 必过项 2：主人口令

| 步骤 | 期望结果 |
|---|---|
| 「我的点单」页下拉刷新 | 出现「主人控制台」，能看到「主人口令：4 位数字」 |
| 点那一行 | 提示已复制 |

> 记下这 4 位口令，或点一下复制存到备忘录。老婆/家人要一起看汇总就靠它。

## 必过项 3：导入菜库并上架

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 进「家宴设置与菜库」→ 点「导入内置菜库」 | 弹窗：新增 69 道，跳过 0 道 | 转圈很久后报超时 → 云函数超时没改成 20 秒 |
| 看页面顶部 | 「已上架 0 / 69」（菜库默认全部下架，等你勾今晚的） | 显示 69/69 → 你点过「全部上架」 |
| 点某个分类的「整组切换」 | 该组菜品的开关整体翻转，顶部数字跟着变 | 数字不变 → 下拉刷新 |
| 把「拍黄瓜」的限量框填 `3`，点一下别处 | 提示"限量 3 份" | — |
| 每道菜名字下面 | 出现 `🧺 缺 N 样`（**只有当库存里记了东西才会显示**，见必过项 7） | — |

**接着按今晚实际菜单调整**：只留 12～14 道，其余关掉。这一步就是"勾选本次上架"。

> 从**旧版本升级**（菜库里已经有菜、但没有食材数据）才需要点「补全食材」——它只补 `ingredients` 字段，不会动你的上架状态和限量。全新导入的菜本来就带食材，不用点。

## 必过项 4：设置截止时间

| 步骤 | 期望结果 |
|---|---|
| 填标题（如"周六家宴"）、地址、给朋友的一句话 | — |
| 截止时间选**今晚往后 10 分钟**（先测截止逻辑用） | 显示"当前：xxxx（还剩 N 分钟）" |
| 点「保存设置」 | 提示"已保存"，回到「点菜」页顶部能看到截止时间 |
| **重要**：测完记得把截止时间改回周六上午，或点「取消截止」 | — |

## 必过项 5：朋友端下单（第二台手机/第二个微信号）

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 扫码进入「点菜」页 | 只看到已上架的菜，能左右滑分类；**往下滑的时候，顶部分类高亮会跟着变** | 看到全部 69 道 → 上架开关没生效，下拉刷新 |
| 给「拍黄瓜」点 `+` 三次 | 底部显示「已点 3 道」，卡片里出现备注输入框 | 底部一直是 0 → 这就是修过的老 bug，说明代码是旧版 |
| **切到另一个分类** | 底部仍然显示「已点 3 道」（不会归零） | 归零 → 同上，代码是旧版 |
| 在拍黄瓜备注里写"多放醋" | — | — |
| 再给手工水饺点 `+` 两次 | 底部「已点 5 道」 | — |
| 点「提交点单」→ 填昵称"测试老王"、人数 2 → 提交 | 提示"点单成功！" | 提示"本场最多点 X 道" → 是设置里的每人上限在起作用，正常 |
| 「我的点单」页 | 能看到明细，"测试老王 · 2 人 · 5 道" | 显示"还没有点单记录" → 下拉刷新 |

## 必过项 6：改单、限量、撤单

| 步骤 | 期望结果 |
|---|---|
| 回「点菜」页，把手工水饺减到 1 份，再提交 | 提示变成"**已更新**你的点单"，不是"点单成功" |
| 「我的点单」页 | 份数已经变了，仍然只有一单 |
| 把拍黄瓜加到 4 份（超过刚设的限量 3）提交 | 被拒绝，提示「拍黄瓜 只剩 3 份了」（或"只剩 N 份"） |
| 改回 3 份以内，提交 | 成功 |
| 点「撤销」→ 确认 | 提示已撤销，「我的点单」变回空 |

> **如果只有一个微信号**：上面每一项都照做，最后一定要点「撤销」，否则真实朋友还没下单时，汇总里就已经有你自己这一单了。

## 必过项 7：家里的库存 🧺（本轮新增）

记的是"我家现在有什么"，跟菜谱无关。

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 「我的点单」→「主人控制台」→「家里的库存」 | 顶部四个数字（共 N 项 / 食材 / 调料 / 其他）和「添加一样」表单 | 提示"未知操作：listPantry" → **云函数没重新部署**，回到开头那一步 |
| 展开「从菜谱食材快速点选」→ 随便点 5～8 个你家里确实有的（生抽、大蒜、鸡蛋…） | 标签变绿带 ✓，上面「共 N 项」跟着涨 | 点了没反应 → 同上 |
| 「批量粘贴」里粘三行：`生抽 2瓶` / `# 注释` / `小葱` → 点「识别并加入」 | 弹窗只列出 2 样（注释行被忽略），确认后加进去 | — |
| 点任意一行 | 上面的表单变成「修改「生抽」」，名称锁住不可改，改数量或备注后点「保存修改」 | — |
| 回「家宴设置与菜库」 | 每道菜名下面出现 `✅ 食材都有` 或 `🧺 缺 N 样`；点一下能看到**具体缺哪几样**，并可「去补库存」 | 一个都没显示 → 库存还是空的（上面一个都没点） |
| 回「后厨看板」→「食材采购」 | 出现「还要买 / 已买 / 家里有」三个数；家里有的带 `家里有` 标记并排在最后 | 家里有的还被算成"还要买" → 下拉刷新 |
| 点「复制待购食材」→ 粘到微信 | 只有还要买的，家里有的没列出来，末尾注明"家里已有的 N 项没列出来" | — |
| 点「清空库存」→ 确认 | 提示已清空，菜单页的"缺 N 样"小字一起消失（库存空的时候不显示） | — |

## 必过项 8：看板与一键复制

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 用第二个微信号下一单（昵称"小李"、3 人、点 2 道菜） | — | — |
| **两个人点同一道菜**：让第一个号点过的菜，第二个号也点一份 | 两个人都能点成功（限量是按人算的，别人点了几份不会挡着你）；看板上这道菜显示「2 人点」 | 提示"只剩 0 份了" → 云函数是旧版（限量还在按全桌算） |
| 主人在「我的点单」→「后厨看板」 | 四个数字：到场人数 / 已下单 / 总份数 / 菜品种类 | 提示"只有主人能看后厨看板" → 你在用非主人号打开 |
| 看「菜品汇总」每一行右边 | **「N 人点 / 共 M 份」**：人数是大字、份数是下面的小字 | 只显示 ×N → 云函数没重新部署 |
| 看「菜品汇总」上方那句提示 | 写着「做多少由你定（下面的份数只是参考）」 | — |
| 看「菜品汇总」 | 每道菜显示份数 + 谁点的；有备注的显示 `↳ 昵称：备注` | 份数不对 → 下拉刷新 |
| 看「按人明细」 | 小李小王各一段，含人数、到达时间、每道菜和备注 | — |
| 用主人号打开「点菜」页（或让朋友打开），展开「大家都在点什么」 | 能看到每道菜共几份、都有谁点了 | 这是朋友之间互相可见的部分，验证一下 |
| 点「复制菜单（发客人）」→ 粘到微信 | 只有菜名和分类，**没有份数、没有谁点的** | — |
| 点「复制菜 + 食材」→ 粘到微信 | 每道菜带份数 + 食材一行 | — |
| 点「预览上次复制的」 | 弹出刚才那份纯文本 | 粘贴/预览是空的 → 再点一次复制 |

## 必过项 9：客人点赞（按场次）👍

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 用朋友号打开「点菜」页 | 顶部有「👍 给家宴的菜点个赞」卡片，写着「N 人参与 · 每场最多 3 道」 | 没这张卡 → 云函数没重新部署 |
| **看菜名后面** | 赞过的菜写着 **`👍 N 人赞`**（跨场次累计）；本场也投过时补一句「· 本场 M」 | 什么都没有 → 一票都没投过（正常），或者云函数没重新部署（页面会提示一句） |
| **看分类栏最前面** | 多了一个 **「👑 招牌」** 标签，点进去是**点赞数前三**的菜（会有一句说明） | 没有这个标签 → 一票都没有（正常）；有票也没有 → 云函数没重新部署 |
| **用一个没下过单的号**（没在这一场点过菜）打开点赞卡片 | 提示「这一场没有你的点单，投不了票」，点菜也点不动 | 能投进去 → 云函数是旧版 |
| 点标题展开 | 列出**这一场点过的菜**（带 `这一场点了 ×N`），下面还有菜单上没点过的菜 | 列表空的 → 这一场还没人点单；空态会提示"这一场还没开席" |
| **先测"没开席就投不了"**：开一场新的（见必过项 10）之后，**在没人点单时**打开这张卡片 | 卡片提示「这一场还没开席（还没有人点单）」，**没有可点的菜**，也发不出票 | 直接能投 → 云函数没重新部署 |
| 让一个人先点一道菜（点了单 = 开席） | 卡片恢复成正常列表，可以点赞了 | — |
| **点一道菜**（不用找提交按钮） | 整行立刻变绿、右侧打勾，右上角出现「保存中…」→「已保存 ✓」；**退出去再进来，票还在**（说明真的存上了） | 一直停在"保存中…" → 云函数没重新部署 |
| 接着再点 2 道 | 每一下点完即存；选到第 4 道时提示"一场最多赞 3 道菜"，第 4 道不会选中 | — |
| **点一下已选中的那道** | 立刻取消并自动保存（不需要任何"提交"动作） | — |
| 回「点菜」页往下看菜品列表 | 被赞的菜名后面出现 `👍N`（这是**跨场次累计**） | — |
| 主人打开「后厨看板」 | 「👍 点赞榜」里有**本场榜**（前三名奖牌+进度条）和 **🏆 累计榜（跨场次）**；「菜品汇总」每行也有 👍N | — |
| 看点赞榜每道菜下面那行 | **列出了是谁赞的**（名字就是朋友点单时用的称呼）；累计榜里同一个人赞过多次会显示 `老王×2` | 显示成"匿名朋友" → 那位朋友投票时和点单时都没留过名字 |
| 看本场榜最下面那行「还没投的：…」 | **这一场点过单、但还没投票的人**都在这里；已经投过的人**不会**出现在里面 | 投过的人也在里面 → 云函数是旧版（这条是按 openid 判的） |
| 让朋友在点赞卡片上点「点这里留个名字」→ 填名字 → 再看主人端 | 名字立刻出现在点赞榜那一行下面 | — |
| 换个朋友号再投 3 道 | 本场榜人数累加（不是同一个人刷上去） | — |
| 同一个号再投 1 道 → 提交 | **覆盖**自己本场那一票（本场榜上这个人还是 1 票） | 这就是"一场一人一票" |
| 点「撤销这一场的」→ 确认 | 这个人从本场榜消失，**累计榜里其他场的票还在** | — |

> **最重要的场景**：把「家宴设置与菜库」里「全部下架」点一下，然后回朋友号点赞 —— **照样能投**。因为候选名单来自"这一场点过的菜"，不是"上架的菜"。吃完家宴通常都会下架，那时才是投票高峰。

## 必过项 10：开新的一场 + 场次回顾 🎊

| 步骤 | 期望结果 | 不通过先看哪里 |
|---|---|---|
| 「后厨看板」→ 顶部「开始新的一场家宴 ›」 | 弹出输入框，默认名是「第 2 场家宴」 | 没这个入口 → 你不是主人 |
| 输入「中秋家宴」→ 确定 | 弹窗说明：现在是第 2 场；上一场订单和点赞都留着；已采购勾选已重置 | — |
| 点顶部「✎ 改这一场的名字 ›」→ 输入「0913场家宴」 | 顶部场次标签变成「🎊 0913场家宴（第 N 场）」；**历史那场的名字没被改** | — |
| 「家宴设置与菜库」→ 点任意菜名右边的 ✎ | 顶部出现编辑面板：菜名/分类/简介/图标/食材/标签都能改；改完点保存，菜往下挪进了新分类 | 没有 ✎ → 云函数没重新部署 |
| 「家宴设置与菜库」→ **🗂 分类整理** | 面板里选「要整理的分类」（比如热菜）→「挪到」选 **按菜名自动归类** → 出一份方案（`· 红烧肉 → 猪肉`）→ 确认整理 | 没有这个按钮 → 云函数没重新部署 |
| 整理完回列表 | 那些菜出现在更具体的分类里，**「热菜」这个分类不见了**；菜名/食材/上架状态都没变 | 菜少了 → 严重问题，先别继续，回来看日志 |
| 看板数字 | **全部归零**（到场人数 / 已下单 / 总份数 / 菜品） | 还有数字 → 那些订单没带 sessionId，检查云函数是否已更新 |
| 顶部场次标签 | 显示「🎊 中秋家宴（第 2 场）」 | — |
| 「👍 点赞榜」 | 本场榜是空的（提示"本场还没人点赞"），**累计榜还在**（上一场的票都还在） | 累计榜也空了 → 严重问题，先别继续，回来看日志 |
| 「家宴设置与菜库」 | 顶部仍写着累计点赞最受欢迎的几道；菜名后面的 👍N 都还在 | — |
| 让上次那位朋友再打开「点菜」→ 展开点赞卡片 | 上方出现**场次标签**：`中秋家宴`（进行中）、`第 1 场家宴`（已投 ✓） | 只有一个标签 → 他这一场还没点过单，也不影响（当前场次永远可选） |
| 点那个旧场次的标签 | 列表切成**那一场吃到的菜**，并显示"你在这一场赞过：…"，可以改了再提交 | — |
| **让同一个人在本场再给同一道菜点赞** | 提交成功；**累计榜上这道菜变成 2 票**（两场各一票） | 这就是「来过多次可以重复点赞」 |
| 用一个从没参加过的号试投第 1 场 | 被拒绝：「这一场家宴你没参加过，投不了票」 | — |
| **看板顶部那一排场次标签**：点「第 1 场家宴」 | 点单明细和点赞榜都换成**第 1 场的**（能看到那一场谁点了什么、谁赞了哪道菜）；顶部标签带「📜 …（历史 · 只看不改）」，右上变成「← 回到当前这一场」 | 只有一个标签 → 你还没开过第二场 |
| 在历史场次里往下看 | 「不做」和改份数的按钮**都收起来了**（历史只看不改）；「食材采购」那块变成一句说明 | 还能点「不做」→ 云函数是旧版 |
| 点「← 回到当前这一场」 | 数字回到当前这一场（第 2 场，还没人点单时是空的） | — |

## 必过项 11：截止与体验版

| 步骤 | 期望结果 |
|---|---|
| 去「家宴设置与菜库」把截止时间设为**1 分钟后** → 保存 | — |
| 等过点，回「点菜」页 | 顶部出现「⏰ 点单已截止」，点 `+` 和「提交点单」都被拒绝 |
| 但「我的点单」仍能查看自己的单 | 能看到 |
| **再试一次点赞** | 截止了照样能点赞（点赞不受截止限制） |
| 把截止时间改回周六上午 10:00 → 保存 | 提示已保存，点单恢复可用 |
| 开发者工具「上传」→ 后台「版本管理」→ 选为体验版本 | 生成体验版二维码 |
| 把一个朋友加为**体验成员**，发二维码给他 | 他能正常进入并点单 |

---

## Go / No-Go 判定

全部 11 项必过 → **发码给朋友**。

任意一项不过 → **先按清单里的"不通过先看哪里"排查**；如果 5 分钟内搞不定，先把 `兜底方案.md` 里的接龙消息发到群里，再继续修。不要因为工具问题耽误一桌人的晚饭。

---

## 自测记录

| # | 项目 | 结果 | 备注 |
|---|---|---|---|
| 1 | 首次开通 | ☐ 通过 ☐ 失败 | |
| 2 | 主人口令 | ☐ 通过 ☐ 失败 | 口令：______ |
| 3 | 导入菜库并上架 | ☐ 通过 ☐ 失败 | 上架 ____ 道 |
| 4 | 设置截止时间 | ☐ 通过 ☐ 失败 | |
| 5 | 朋友端下单 | ☐ 通过 ☐ 失败 | |
| 6 | 改单/限量/撤单 | ☐ 通过 ☐ 失败 | |
| 7 | 家里的库存 | ☐ 通过 ☐ 失败 | 记了 ____ 项 |
| 8 | 看板与一键复制 | ☐ 通过 ☐ 失败 | |
| 9 | 客人点赞（按场次） | ☐ 通过 ☐ 失败 | ____ 人参与 |
| 10 | 开新的一场 + 场次回顾 | ☐ 通过 ☐ 失败 | 第 ____ 场 |
| 11 | 截止与体验版 | ☐ 通过 ☐ 失败 | |

自测人：__________　时间：__________
